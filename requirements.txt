# Listenr dependencies
# All ASR and LLM inference is handled by Lemonade Server (https://lemonade-server.ai)
# No local model packages needed.

# HTTP / WebSocket client
requests==2.32.5
websockets

# Audio capture and processing
sounddevice==0.5.1
soundfile==0.13.1
numpy==2.2.5
scipy

# Dataset building
pandas
